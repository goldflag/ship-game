import type { CloudMarchParams } from "../state/CloudQuality";
/**
 * Initial primary + light march settings for the on-screen cloud raymarch. Not tier-scaled —
 * `setQualityLevel()` leaves these alone. See {@link CloudMarchParams} for each field.
 */
export declare const CLOUD_MARCH_SETTINGS: CloudMarchParams;
