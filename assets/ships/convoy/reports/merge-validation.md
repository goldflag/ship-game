# Convoy merge integration — 6 September 2026

The reviewed ship work is commit `af21182ccd098fe64e408aa759039db73bbc10c2`. Commit `b731604` integrates the existing fleet work through `cc02370` (the captured-plan Type VIIC reconstruction). The only source merge conflict was port equipment text: the resolution preserves both submarine diving descriptions and definition-derived merchant machinery/armor descriptions.

## Rebuilt assets

All four convoy ships were rebuilt through `ship:build` with local Blender 5.2 because the newer shared compiler/recipes invalidate their earlier export hashes. No Blender MCP tools were available. All four `ship:check` validations pass. Fixed five-view reviews and the three transparent same-scale plan comparisons were regenerated from the rebuilt sources.

| Preset | Current definition/recipe hash |
| --- | --- |
| Liberty Cargo | `7f550a85e52db6553dd6bc90a1bdc77ac0810145971a8e65719daf3887938430` |
| Liberty Collier | `4a2c183dece9c55b6e81477c2e1d229a0618765eb5d13d2a77488f81f2a008c0` |
| Victory Cargo | `1b7774a4768bd2a4063e17ded52f5ccc28c00383db1416d8243f8cc0f326612c` |
| Flower Corvette | `e667b8ae2dca5baa7493c047eb106d2c3557bdd8ac6d1605fc0c2c75a4b322d2` |

[Continuity evidence](merge-continuity.json), generated by [the read-only verifier](../verify-merge-continuity.ts), compares the actual earlier and rebuilt GLBs and compiled definitions. Every simulation definition and scene document is identical after excluding the definition stamp. Positions, normals, triangle indices, materials and independent joint/socket hierarchies are unchanged. Cargo, Victory and Flower have identical binary chunks. Collier has eight UV float-rounding differences of at most `5.960464477539063e-8`; all other binary bytes are identical. Triangle counts and export sizes remain those in the original validation report.

Yamato, Baltimore, Enterprise and Type VIIC comparison packs were regenerated with `ship:compare`, since their historical-only evidence hashes include the shared catalog and pipeline. Their playable geometry and original references were not edited. Bismarck's preserved baseline remains unchanged.

## Checks

- `bun test src/game/ShipView.test.ts src/simulation/convoy.test.ts`: **39 passed, zero failed; 13,029 assertions**, 4.68 seconds after all four rebuilds.
- `bun run build`: **passed** all nine ship checks, thirteen aircraft checks, TypeScript and Vite production output. The existing large game-bundle warning remains (10.96 MB after integration).
- The full `bun test` run recorded **455 passes and four timeouts** across 459 tests. It did not produce assertion failures. Rendering/other host activity was present during that run.
- After ship builds finished, the four timed cases were rerun with their unchanged timeout settings. Yamato six-sided flooding coverage, submerged gun/torpedo behavior and bot opening delay passed. `Yamato wing spaces connect damaged exterior openings to retained turbine rooms` still exceeded its 5-second limit (11.06 seconds).

A clean full-suite pass is therefore not claimed. No assertions, test timeouts, sleep/thermal protections or unrelated processes were changed to obtain a pass.

## Evidence limits and merge destination

This is an integration rebuild, not a new historical-accuracy approval or live-browser review. The earlier in-game captures retain their recorded hashes and limitations. Export continuity establishes that the reviewed ship geometry and articulating parts did not change; it does not manufacture a new browser run.

At the final target check, `/Users/bill/Desktop/ship-game` was in an unrelated interactive rebase of `master` onto `a9daaec`, replaying `6f85684` (Enterprise aircraft deployment). It had conflicts in `src/game/Game.ts` and `src/simulation/combat.ts`. That checkout was not modified or reset by this task. The convoy branch is retained for merging after the main-checkout operation is resolved; no remote push is implied by this report.
