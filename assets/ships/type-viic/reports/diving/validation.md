# Diving validation · 2026-09-05

The Type VIIC now dives through CPU ballast and plane controls. Ballast is separate from damage water; `motion.y` is the single authoritative hull height. Tank rates, depth limits and pressure damage are explicit gameplay approximations, recorded in the discrepancy register. Captured U-570 evidence is the retained ONI report and the linked British report in the source register.

- Shared compiler/blueprint validation, all five `ship:build` outputs, five `ship:check` validations and production build passed. No Blender MCP was available; local Blender performed the build and fixed-view review.
- Inspected profile, plan, bow, stern and quarter renders under `generated/review/`. Exterior geometry remains the original reconstruction. Eight existing appendage pivots are now linked from the definition and validated as independent empties with moving children.
- Full suite: 297 passing tests across 40 files, 33,202 assertions. The subsequent camera-framing revision passed the 33 relevant camera/game/aiming/HUD tests and a fresh production build.
- Regression coverage includes gradual filling; 7 m / 50 m depth hold at rest, ahead and astern; normal/emergency surfacing; diesel/electric damage isolation; submerged gun inhibition; a torpedo fired at 7 m hitting a surfaced battleship; launch limits and exposed mouths; shallow torpedoes and hulls passing above a deep boat; pressure flooding/damage; sunk hull continuity; reset; invalid orders; bot depth behavior; display-rate determinism; control migration; camera modes; and actual exported appendage articulation.
- Runtime review uses the Orca embedded browser at `http://127.0.0.1:5197/?ship=type-viic&review=diving`. The development-only `shipTrialAdvance(seconds)` rehearsal can advance up to 120 seconds through the same fixed CPU ticks when render throughput is slow. It is disabled in production and does not change game speed, control orders or physical rules.
- The first visual pass found an underwater follow distance beyond visibility and a mobile depth-panel overlap with the sight. The revision moves the camera just behind the stern and narrows the mobile panel. The detector reported 11 advisory type/palette findings, with no blocking findings; incumbent compact naval typography is retained.
- Final runtime evidence is in `runtime.json`: the boat holds 7 m with 85% ballast and electric propulsion; the periscope eye stays 1.4 m above mean water level; a 50 m order reaches 49.99999 m and secures all torpedo tubes. Pressing B at that depth orders zero metres, empties the tanks within five seconds, and raises the hull at the configured 2.5 m/s limit. By the end of the 65-second rehearsal the boat is surfaced, emergency mode has cleared and diesel propulsion is restored. Enemy gunfire damages the boat after it surfaces.
- Final HUD captures are `desktop.png` (1137 × 906) and `mobile.png` (390 × 844). `periscope.png` records the above-water view at periscope depth. The independent finish review requested a clear mobile aiming prompt and correction of the stale surface-only product description. Both were resolved; final reviewer disposition: **ship**. The final production build passed after these fixes.

Build evidence establishes valid exports and behavior, not historical accuracy. Battery endurance, finite compressed air, oxygen, sonar and depth charges remain outside this feature.
