# Shared fidelity tools

Shared tools from the original fleet geometry work, not a ship preset. Current geometry is authored by per-ship `build.py` and shared original components under `scripts/ships/`.

The one-off before/after authoring and comparison archive has been removed. Retained runtime helpers can inspect a development game; keep output local in `.build/`. Do not restore historical report snapshots as production inputs.
