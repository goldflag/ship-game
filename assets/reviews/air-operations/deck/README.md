# Visible carrier deck cycle

Aircraft use the existing Wildcat, Dauntless and Devastator models. Ready, queued and rearming aircraft remain visible in port and battle. Taxi, takeoff run, final approach, touchdown, three-second rollout and return to a stable parking spot are CPU phases. The same inventory survives the cycle. Recall cancels unlaunched orders and returns survivors; servicing takes 35 seconds after parking.

The Enterprise blueprint moves launch/recovery X to 4 m. The derived parking row is at X=-7 m, clear of the island. Local Blender rebuilt Enterprise through `ship:build`; no Blender MCP was available and no aircraft geometry was changed. The aircraft follow interpolated hull transforms on deck. Parked props stop and gear stays down. The diagnostic `/scripts/diagnostics/carrier-deck.html` uses the real aircraft adapter and CPU phases over the published ship for repeatable orbit/launch/recall inspection.

Checks: 34 targeted aircraft, renderer, fleet-loading and frame-loop tests passed, plus 14 aircraft tests including the added complete recalled-group recovery regression. `bun run build` passed all ship/aircraft checks, TypeScript and Vite. Browser inspection found an oversized payload uniform allocation; payload batches now stay below 64 KiB and omit stowed weapons. The current renderer checks cover deck instance counts, loss removal, port filtering and transformed parking positions.

Approximations remain: compact unfolded-wing spotting, wing-tip overlap with the operating lane, kinematic taxi/approach, no physical deck collision solver, arresting cable or elevators. Previous historical comparison captures retain their original hashes and do not certify this export. No historical-accuracy claim is made.

Final WebGPU diagnostic review of export `026068f84d7d94c39c83be88b5e285e93a486a425af6ed6899e8e70aae08bc72` retained `parked.png`, `launch.png` and `landing.png`. A launched and recalled three-fighter group produced three recovery events and all 18 inventory planes finished ready; no captured GPU validation error occurred (`runtime.json`). These are the dedicated flight-cycle scene, not ocean-scene screenshots. The final ocean startup check was not completed; earlier port diagnostics confirmed 18 rendered instances. Empty LOD/payload batches now start hidden to avoid unnecessary shader compilation during startup.
