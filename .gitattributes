# Keep original source assets and references binary in diffs and merges.
*.blend binary
*.glb binary
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary

# Install the repository-local driver with bun run git:setup after cloning.
# Real same-field conflicts remain unmerged; binary outputs keep normal Git behavior.
assets/parts/guns.json merge=ship-catalog
# Archival references, validation reports and review captures live in Git LFS. They are pipeline
# inputs/evidence, never runtime assets; pipeline hashing accepts LFS pointers, so deployment hosts
# can clone with GIT_LFS_SKIP_SMUDGE=1. Text records stay in plain Git for diffs.
assets/ships/*/references/** filter=lfs diff=lfs merge=lfs -text
assets/ships/*/reports/** filter=lfs diff=lfs merge=lfs -text
assets/ships/*/baseline/** filter=lfs diff=lfs merge=lfs -text
assets/reviews/** filter=lfs diff=lfs merge=lfs -text
assets/aircraft/reports/** filter=lfs diff=lfs merge=lfs -text
assets/maps/review/** filter=lfs diff=lfs merge=lfs -text
assets/effects/*/review/** filter=lfs diff=lfs merge=lfs -text
assets/effects/*/reports/** filter=lfs diff=lfs merge=lfs -text
assets/effects/*/references/** filter=lfs diff=lfs merge=lfs -text
assets/**/*.md !filter !diff !merge text
assets/**/*.json !filter !diff !merge text
assets/**/*.txt !filter !diff !merge text
assets/**/*.csv !filter !diff !merge text
assets/**/*.py !filter !diff !merge text
assets/**/*.ts !filter !diff !merge text
assets/**/*.svg !filter !diff !merge text
